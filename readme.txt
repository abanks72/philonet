python -m venv venv for my-flask app

venv\Scripts\activate upon every instance of terminal (windowd cmd)

Dependencies:
pip install -r requirements.txt

running the app 
python app.py

pip install gunicorn
^ for automatic reload whenever you make code changes, eliminating the need to manually restart the server. (local development)
testagain